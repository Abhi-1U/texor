% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/subfigure-tools.R
\name{patch_subfigure_env}
\alias{patch_subfigure_env}
\title{Patch subfigure environments in a LaTeX file}
\usage{
patch_subfigure_env(
  file,
  output_file = file,
  ref_commands = "ref",
  backup = TRUE
)
}
\arguments{
\item{file}{Path to the LaTeX file to modify.}

\item{output_file}{Path to the output LaTeX file. Defaults to `file`,
so the input file is modified in place.}

\item{ref_commands}{Character vector of LaTeX reference commands to
rewrite. Defaults to `"ref"`.}

\item{backup}{Logical; if `TRUE` and `output_file` is the same as
`file`, create `file.bk` before modifying the file.}
}
\value{
Invisibly returns the path to the converted LaTeX file.
}
\description{
Converts `subfigure` environments contained in LaTeX figures into
`tabular` layouts using the Lua converter shipped with texor.
}
\examples{
\dontrun{
tex_file <- "article.tex"
patch_subfigure_env(tex_file)

# Or write to a separate file
patch_subfigure_env(
  tex_file,
  output_file = "article-converted.tex"
)

# Rewrite additional cross-reference commands
patch_subfigure_env(
  tex_file,
  ref_commands = c("ref", "autoref", "cref", "Cref")
)
}
}
